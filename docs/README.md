For example:

A database table exists because an entity needs it.
An entity exists because an aggregate needs it.
An aggregate exists because a bounded context owns it.
A bounded context exists because it supports a business capability.
A business capability exists because it supports the product strategy.
The product strategy exists to execute the business model, which fulfills the mission and moves the company toward its vision.